# C++ 소스코드 파일은 LF 줄바꿈 사용 (Linux 스타일)
*.cpp text eol=lf

# C/C++ 헤더 파일도 LF 줄바꿈 강제
*.h   text eol=lf
*.hpp text eol=lf

# C 소스코드도 LF 줄바꿈 사용
*.c   text eol=lf

# 설정 파일 (예: Visual Studio 관련)

# Visual Studio 솔루션 파일은 LF 줄바꿈 사용
*.sln text eol=lf

# Visual C++ 프로젝트 파일은 LF 줄바꿈 사용
*.vcxproj text eol=lf

# Visual Studio 프로젝트 필터 파일은 LF 줄바꿈 사용
*.filters text eol=lf

# 바이너리 파일은 Git이 텍스트로 다루지 않도록
*.pdb binary
*.exe binary
*.dll binary
*.lib binary
*.obj binary
*.png binary
*.jpg binary